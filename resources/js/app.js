// Main application JavaScript file\n\n// Import CSS first\nimport '../css/app.css';\n\n// Then import JavaScript modules\nimport Alpine from 'alpinejs';\nimport './components/darkMode';\nimport './components/navbar';\nimport './utils/animations';\nimport { logger, ENV } from './helpers/constants';\nimport { dom, storage, screen } from './helpers/utils';\n\n// Make Alpine available globally\nwindow.Alpine = Alpine;\n\n// Make utilities available globally for debugging\nif (ENV.isDev) {\n  window.appUtils = { dom, storage, screen };\n}\n\n// Initialize Alpine\nAlpine.start();\n\n// Log application start\nlogger.log('🚀 CI4 Tailwind Template loaded successfully!');\nlogger.log('Environment:', ENV.isDev ? 'Development' : 'Production');\nlogger.log('Current breakpoint:', screen.getCurrentBreakpoint());\n\n// Global error handler\nwindow.addEventListener('error', (event) => {\n  logger.error('Global error:', event.error);\n});\n\n// Unhandled promise rejection handler\nwindow.addEventListener('unhandledrejection', (event) => {\n  logger.error('Unhandled promise rejection:', event.reason);\n});