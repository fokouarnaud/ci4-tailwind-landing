// Constants for the application\nexport const APP_CONFIG = {\n  // Breakpoints (match Tailwind's defaults)\n  BREAKPOINTS: {\n    sm: 640,\n    md: 768,\n    lg: 1024,\n    xl: 1280,\n    '2xl': 1536,\n  },\n  \n  // Animation durations\n  ANIMATION: {\n    fast: 150,\n    normal: 300,\n    slow: 500,\n  },\n  \n  // Local storage keys\n  STORAGE_KEYS: {\n    theme: 'theme',\n    language: 'language',\n    preferences: 'user_preferences',\n  },\n  \n  // Theme values\n  THEMES: {\n    light: 'light',\n    dark: 'dark',\n    auto: 'auto',\n  },\n  \n  // Supported languages\n  LANGUAGES: {\n    fr: 'fr',\n    en: 'en',\n  },\n};\n\n// Environment detection\nexport const ENV = {\n  isDev: window.location.hostname === 'localhost',\n  isProd: window.location.hostname !== 'localhost',\n  isMobile: /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent),\n  isTablet: /iPad/i.test(navigator.userAgent),\n  isDesktop: !/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent),\n  supportsTouch: 'ontouchstart' in window,\n  prefersReducedMotion: window.matchMedia('(prefers-reduced-motion: reduce)').matches,\n  prefersDarkMode: window.matchMedia('(prefers-color-scheme: dark)').matches,\n};\n\n// Console logger with different levels\nexport const logger = {\n  log: (...args) => {\n    if (ENV.isDev) console.log('[APP]', ...args);\n  },\n  warn: (...args) => {\n    if (ENV.isDev) console.warn('[APP]', ...args);\n  },\n  error: (...args) => {\n    console.error('[APP]', ...args);\n  },\n  debug: (...args) => {\n    if (ENV.isDev) console.debug('[APP]', ...args);\n  },\n};